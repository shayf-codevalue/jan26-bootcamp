// TASK: Use Ctrl+D to fix the misspelled variable 'user_name'
function greetUser() {
    const user_name = "Junior Dev";
    console.log("Welcome to the team, " + user_name)

    console.log("Access granted for: " + user_name);
}

// TASK: Use Alt + Up/Down to move this line inside the function above

greetUser();
