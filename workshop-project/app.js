// TASK: Use Ctrl+D to fix the misspelled variable 'user_nme'
function greetUser() {
    const user_nme = "Junior Dev";
    console.log("Welcome to the team, " + user_nme);
}

// TASK: Use Alt + Up/Down to move this line inside the function above
console.log("Access granted for: " + user_nme);

greetUser();
